<% if @pet.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@pet.errors.count, "error") %> prohibited this pet from being saved:</h2>

  <ul>
  <% @pet.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
  </ul>
</div>
<% end %>

<h2 class ="text-center">Edit <%= @pet.name %>'s Profile</h2>

<%= simple_form_for @pet, html: { multipart: true } do |f| %>
  <%= f.input :name, required: true, label: "Pet Name:"  %>
  <%= f.file_field :profile_photo, type: :file %>
  <%= f.input :breed, required: true, label: "Breed:" %>
  <%= f.input :age, required: true %>
  <%= f.input :weight, required: true %>
  <%= f.input :personality, as: :text %>
  <%= f.input :favorite_things, as: :text %>
  <%= f.button :submit, 'Save Changes' %>
  <%= f.button :submit, 'Delete Pet' %>
<% end %>



